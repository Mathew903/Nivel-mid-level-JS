let expr = 'manzanadsasdasdsa';

switch (expr) {
	case 'banana':
		console.log('Esto es una banana');
		break;
	case 'pera':
		console.log('Esto es una pera');
		break;
	case 'manzana':
		console.log('Esto es una manzana');
		break;
	default:
		console.log('Es otra cosa');
}

/* Sentencia Swicht

? Sentencia de control de flujo

* sintaxis y clausula case

let expr = 'manzana';

switch (expr) {
	case 'banana':
		console.log('Esto es una banana');
		break;
	case 'pera':
		console.log('Esto es una pera');
		break;
	case 'manzana':
		console.log('Esto es una manzana');
		break;
}

Con la sentencia switch no hace falta usar llaves y ademas no es necesario colocar una condicion, sino que con solo colocar que queremos q se ejecute, ya està. Pero if tiene un mejor rendimiento.

* break
es para que lo demas del codigo no se ejecute y solo se ejecute lo que llame.


* default

Pero si quisiesemos excepciones en swicht, tendriamos que usar "default" basicamente es, si nada de lo anterior se cumple y el valor no es igual a nada de lo anterior, nos ejecuta otra cosa.

let expr = 'manzana';

switch (expr) {
	case 'banana':
		console.log('Esto es una banana');
		break;
	case 'pera':
		console.log('Esto es una pera');
		break;
	case 'manzana':
		console.log('Esto es una manzana');
		break;
	default:
		console.log('Es otra cosa');
}

*/
